[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

# Python linting/formatting moved to ruff (v3.0.0).
# Consumer repos configure ruff via their own ruff.toml.
